{% set client_type = get_client_type(spec) %}
#![allow(unused_imports)]
#![allow(clippy::too_many_arguments)]

{% if ffi %} uniffi::setup_scaffolding!(); {% endif %}

pub mod apis;
pub mod models;
{% if client_type == "Algod" and not ffi %}
pub mod msgpack_value_bytes;
{% endif %}

// Re-export the main client for convenience
pub use apis::{{ client_type }}Client;

