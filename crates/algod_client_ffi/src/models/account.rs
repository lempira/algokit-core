/*
 * Algod REST API.
 *
 * API endpoint for algod operations.
 *
 * The version of the OpenAPI document: 0.0.1
 * Contact: contact@algorand.com
 * Generated by: Rust OpenAPI Generator
 */

use crate::models;
use algod_client::models::Account as RustAccount;
use algokit_transact_ffi::SignedTransaction as AlgokitSignedTransaction;

use crate::models::AccountParticipation;
use crate::models::Application;
use crate::models::ApplicationLocalState;
use crate::models::ApplicationStateSchema;
use crate::models::Asset;
use crate::models::AssetHolding;

/// Account information at a given round.
///
/// Definition:
/// data/basics/userBalance.go : AccountData
#[derive(Clone, Debug, PartialEq, uniffi::Record)]
pub struct Account {
    /// the account public key
    pub address: String,
    /// [algo] total number of MicroAlgos in the account
    pub amount: u64,
    /// MicroAlgo balance required by the account.
    ///
    /// The requirement grows based on asset and application usage.
    pub min_balance: u64,
    /// specifies the amount of MicroAlgos in the account, without the pending rewards.
    pub amount_without_pending_rewards: u64,
    /// [appl] applications local data stored in this account.
    ///
    /// Note the raw object uses `map[int] -> AppLocalState` for this type.
    pub apps_local_state: Option<Vec<ApplicationLocalState>>,
    /// The count of all applications that have been opted in, equivalent to the count of application local data (AppLocalState objects) stored in this account.
    pub total_apps_opted_in: u64,
    pub apps_total_schema: Option<ApplicationStateSchema>,
    /// [teap] the sum of all extra application program pages for this account.
    pub apps_total_extra_pages: Option<u64>,
    /// [asset] assets held by this account.
    ///
    /// Note the raw object uses `map[int] -> AssetHolding` for this type.
    pub assets: Option<Vec<AssetHolding>>,
    /// The count of all assets that have been opted in, equivalent to the count of AssetHolding objects held by this account.
    pub total_assets_opted_in: u64,
    /// [appp] parameters of applications created by this account including app global data.
    ///
    /// Note: the raw account uses `map[int] -> AppParams` for this type.
    pub created_apps: Option<Vec<Application>>,
    /// The count of all apps (AppParams objects) created by this account.
    pub total_created_apps: u64,
    /// [apar] parameters of assets created by this account.
    ///
    /// Note: the raw account uses `map[int] -> Asset` for this type.
    pub created_assets: Option<Vec<Asset>>,
    /// The count of all assets (AssetParams objects) created by this account.
    pub total_created_assets: u64,
    /// [tbx] The number of existing boxes created by this account's app.
    pub total_boxes: Option<u64>,
    /// [tbxb] The total number of bytes used by this account's app's box keys and values.
    pub total_box_bytes: Option<u64>,
    pub participation: Option<AccountParticipation>,
    /// Whether or not the account can receive block incentives if its balance is in range at proposal time.
    pub incentive_eligible: Option<bool>,
    /// amount of MicroAlgos of pending rewards in this account.
    pub pending_rewards: u64,
    /// [ebase] used as part of the rewards computation. Only applicable to accounts which are participating.
    pub reward_base: Option<u64>,
    /// [ern] total rewards of MicroAlgos the account has received, including pending rewards.
    pub rewards: u64,
    /// The round for which this information is relevant.
    pub round: u64,
    /// [onl] delegation status of the account's MicroAlgos
    ///   * Offline - indicates that the associated account is delegated.
    ///   *  Online  - indicates that the associated account used as part of the delegation pool.
    ///   *   NotParticipating - indicates that the associated account is neither a delegator nor a delegate.
    pub status: String,
    /// Indicates what type of signature is used by this account, must be one of:
    ///   * sig
    ///   * msig
    ///   * lsig
    pub sig_type: Option<String>,
    /// [spend] the address against which signing should be checked. If empty, the address of the current account is used. This field can be updated in any transaction by setting the RekeyTo field.
    pub auth_addr: Option<String>,
    /// The round in which this account last proposed the block.
    pub last_proposed: Option<u64>,
    /// The round in which this account last went online, or explicitly renewed their online status.
    pub last_heartbeat: Option<u64>,
}

impl From<RustAccount> for Account {
    fn from(rust_struct: RustAccount) -> Self {
        Self {
            address: rust_struct.address,
            amount: rust_struct.amount,
            min_balance: rust_struct.min_balance,
            amount_without_pending_rewards: rust_struct.amount_without_pending_rewards,
            apps_local_state: rust_struct
                .apps_local_state
                .map(|v| v.into_iter().map(|v| v.into()).collect()),
            total_apps_opted_in: rust_struct.total_apps_opted_in,
            apps_total_schema: rust_struct.apps_total_schema.map(|v| v.into()),
            apps_total_extra_pages: rust_struct.apps_total_extra_pages,
            assets: rust_struct
                .assets
                .map(|v| v.into_iter().map(|v| v.into()).collect()),
            total_assets_opted_in: rust_struct.total_assets_opted_in,
            created_apps: rust_struct
                .created_apps
                .map(|v| v.into_iter().map(|v| v.into()).collect()),
            total_created_apps: rust_struct.total_created_apps,
            created_assets: rust_struct
                .created_assets
                .map(|v| v.into_iter().map(|v| v.into()).collect()),
            total_created_assets: rust_struct.total_created_assets,
            total_boxes: rust_struct.total_boxes,
            total_box_bytes: rust_struct.total_box_bytes,
            participation: rust_struct.participation.map(|v| v.into()),
            incentive_eligible: rust_struct.incentive_eligible,
            pending_rewards: rust_struct.pending_rewards,
            reward_base: rust_struct.reward_base,
            rewards: rust_struct.rewards,
            round: rust_struct.round,
            status: rust_struct.status,
            sig_type: rust_struct.sig_type,
            auth_addr: rust_struct.auth_addr,
            last_proposed: rust_struct.last_proposed,
            last_heartbeat: rust_struct.last_heartbeat,
        }
    }
}

impl From<Account> for RustAccount {
    fn from(ffi_struct: Account) -> Self {
        Self {
            address: ffi_struct.address,
            amount: ffi_struct.amount,
            min_balance: ffi_struct.min_balance,
            amount_without_pending_rewards: ffi_struct.amount_without_pending_rewards,
            apps_local_state: ffi_struct
                .apps_local_state
                .map(|v| v.into_iter().map(|v| v.into()).collect()),
            total_apps_opted_in: ffi_struct.total_apps_opted_in,
            apps_total_schema: ffi_struct.apps_total_schema.map(|v| v.into()),
            apps_total_extra_pages: ffi_struct.apps_total_extra_pages,
            assets: ffi_struct
                .assets
                .map(|v| v.into_iter().map(|v| v.into()).collect()),
            total_assets_opted_in: ffi_struct.total_assets_opted_in,
            created_apps: ffi_struct
                .created_apps
                .map(|v| v.into_iter().map(|v| v.into()).collect()),
            total_created_apps: ffi_struct.total_created_apps,
            created_assets: ffi_struct
                .created_assets
                .map(|v| v.into_iter().map(|v| v.into()).collect()),
            total_created_assets: ffi_struct.total_created_assets,
            total_boxes: ffi_struct.total_boxes,
            total_box_bytes: ffi_struct.total_box_bytes,
            participation: ffi_struct.participation.map(|v| v.into()),
            incentive_eligible: ffi_struct.incentive_eligible,
            pending_rewards: ffi_struct.pending_rewards,
            reward_base: ffi_struct.reward_base,
            rewards: ffi_struct.rewards,
            round: ffi_struct.round,
            status: ffi_struct.status,
            sig_type: ffi_struct.sig_type,
            auth_addr: ffi_struct.auth_addr,
            last_proposed: ffi_struct.last_proposed,
            last_heartbeat: ffi_struct.last_heartbeat,
        }
    }
}
